add more posts
    delete temp seeder
    App\Livewire\Blog
        increase paginate

integrate posts to /

add post page to view post

change title based on route
    ? using component attribute

finish home page

change PaymentSeeder to match all user
