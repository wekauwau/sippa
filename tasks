add D Bill

fix(Stu...Action): getActions() EditAction mutateRecordDataUsing
    closure parameter should: array $data
    instead of: <Model> $<var>

R Bill: add toggle (syahriah, ...)

?nav menu: route group: active indicator

roles for managers (bendahara)

upload image

fix post content:
    add break space

add gallery page

change title based on route
    ? using component attribute

finish home page: UI, responsive

change PaymentSeeder to match all user

remove username: login with phone, password

move active from User to Student

?use real data

PaymentSeeder match BillSeeder
